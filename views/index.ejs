<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Talk Hub - Startseite</title>
    <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
    <header>
        <h1>Willkommen bei Talk Hub!</h1>
        <nav>
            <a href="/">Start</a> |
            <% if (user) { %>
            <span>Eingeloggt als <%= user.username || user.displayName || user.email %></span> |
            <a href="/logout">Logout</a>
            <% } else { %>
            <a href="/auth/google">Login</a> |
            <a href="/auth/google/register">Register with another Google account</a> |
            <% } %>
        </nav>
         <% if (user) { %>
        <div style="position: absolute; top: 20px; right: 30px;">
            <a href="/profile" style="background: #222; color: #a0c8ff; padding: 8px 16px; border-radius: 6px; text-decoration: none;">Profil</a>
        </div>
        <% } %>
    </header>

    <main>
        <section>
            <h2>Dein neues Online-Forum</h2>
            <p>Hier kannst du Beiträge posten, kommentieren und liken.</p>
        </section>
    </main>

    <footer>
        <p>© 2025 Talk Hub</p>
    </footer>
</body>
</html>