<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Profil bearbeiten</title>
  <link rel="stylesheet" href="/css/styles.css" />
  <style>
    .form-group { margin-bottom: 24px; }
    .back-btn {
      display: inline-block;
      margin-bottom: 24px;
      background: #222;
      color: #a0c8ff;
      padding: 8px 16px;
      border-radius: 6px;
      text-decoration: none;
      transition: background 0.2s;
    }
    .back-btn:hover {
      background: #444;
    }
  </style>
</head>
<body>
  <div class="container mx-auto max-w-xl p-4">
    <a href="/" class="back-btn">← Zurück zur Startseite</a>
    <h1 class="text-2xl font-bold mb-4">Profil bearbeiten</h1>

    <% if (msg) { %>
      <div class="bg-red-200 p-2 mb-4 rounded"><%= msg %></div>
    <% } %>

    <form action="/profile" method="POST" class="space-y-4">
      <div class="form-group">
        <label class="block font-semibold">Benutzername</label>
        <input type="text" name="username" value="<%= profil.username || '' %>"
               class="w-full border p-2 rounded" required>
      </div>

      <div class="form-group">
        <label class="block font-semibold">Beschreibung</label>
        <textarea name="beschreibung" rows="4"
                  class="w-full border p-2 rounded"><%= profil.beschreibung || '' %></textarea>
      </div>

      <button class="bg-blue-600 text-white px-4 py-2 rounded">Speichern</button>
    </form>
  </div>
</body>
</html>